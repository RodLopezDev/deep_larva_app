package com.iiap.deeplarva.gob.pe.domain.constants

class CloudKeysConstants {
    companion object {
        const val LAST_DATE_CHECKED = "cloud-last-date-checked"
        const val ERROR_REQUEST = "cloud-error-request"
        const val APP_VERSION = "cloud-app-version"
        const val SERVER_URL = "cloud-server-url"
        const val SERVER_API_KEY = "cloud-server-api-key"

        const val FLAG_CAMERA_CONFIG = "cloud-camera-config"
        const val FLAG_CAMERA_CONFIG_EXIST = "cloud-camera-config-exists"
        const val ISO_VALUE = "cloud-iso-value"
        const val EXPOSURE_VALUE = "cloud-exposure-value"
        const val SHUTTER_SPEED_VALUE = "cloud-shutter-speed-value"
    }
}